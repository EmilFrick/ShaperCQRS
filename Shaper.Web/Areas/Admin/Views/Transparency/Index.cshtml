@model IEnumerable<TransparencyDisplayVM>

@if (Model != null)
{

    <div class="container">
        <div class="row pt-4 my-2">
            <div class="col-4">
                <h2 class="text-primary">Transparency List</h2>
            </div>
            <div class="col-8 text-end">
                <a asp-action="Upsert" class="btn btn-primary"><i class="bi bi-folder-plus">&nbsp; New Transparency</i></a>
            </div>
        </div>
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>@nameof(TransparencyDisplayVM.Name)</th>
                    <th>@nameof(TransparencyDisplayVM.Value)</th>
                    <th>@nameof(TransparencyDisplayVM.Description)</th>
                    <th>@nameof(TransparencyDisplayVM.AddedValue)</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var transparency in Model)
                {
                    <tr>
                        <td>@transparency.Name</td>
                        <td>@transparency.Value</td>
                        <td>@transparency.Description</td>
                        <td>@transparency.AddedValue</td>
                        <td>
                            <div class="w-100 btn-group" role="group">
                                <a asp-action="Upsert" asp-route-id="@transparency.Id" class="btn btn-primary mx-1"><i class="bi bi-pencil-square"></i>&nbsp;&nbsp;Edit</a>
                                <form method="post">
                                    <button type="submit" asp-action="Delete" asp-route-id="@transparency.Id" class="btn btn-secondary"><i class="bi bi-x-circle"></i>&nbsp;&nbsp;Delete</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <p>No transparencies found</p>
}